@model WebEvento.ViewModel.InformationViewModel
@using WebEventoo_DomainClasses.Model;
@{
    ViewData["Title"] = "اطلاعات";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h4>اطلاعات</h4>
<div>
    <div class="account-pages">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-5">
                    <div class="card mb-0">
                        <div class="card-body">
                            <div class="p-2">
                                <form   asp-antiforgery="true">

                                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                                    <div class="form-group row" dir="rtl">
                                        <div class="col-12">
                                            <label asp-for="Information.Caption" class="control-label" dir="rtl" style=" FLOAT: right"></label>
                                            <input asp-for="Information.Caption" class="form-control form-control-success" />
                                            <span asp-validation-for="Information.Caption" class="text-danger"></span>
                                        </div>
                                    </div>
                                    <div class="form-group row">
                                        <div class="col-12">
                                            <label asp-for="Information.Type" class="control-label" style=" FLOAT: right"></label>
                                            <select asp-for="Information.Type" class="form-control" form-control-success>
                                                <option value="0">@Information.TypeInformation.AboutUs.GetDisplayName()</option>
                                                <option value="1">@Information.TypeInformation.Rule.GetDisplayName()</option>



                                            </select>

                                            <span asp-validation-for="Information.Type" class="text-danger"></span>
                                        </div>
                                    </div>

                                    <div class="form-group">
                                        <div class="col-12">
                                            <input type="submit" id="btnsave" value="ذخیره " class="btn btn-danger" />
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>









</div>
@section Script{
    <script type="text/javascript">
                $('#btnsave').click(function(evt) {
  evt.preventDefault();

  var payload = $('form').serialize();

  $.ajax({
    url: '@Url.Action("Information", "Home")',
     type: 'POST',
     data: payload,
     success: function (result) {
         //success
       alert(result);

     },
     error: function (error) {
       alert(error);
     }
  });
        });
    </script>
}
